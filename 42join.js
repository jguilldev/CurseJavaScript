// metodo join en arrays
// es un metodo de array que no modifica e array original, toma todos los elementos del array y retorna un solo elemento

const palabras = ['Elon', 'Musk', 'es', 'el', 'fundador', 'de', 'neura', 'link']

const frasecompleta = palabras.join(" ")//recibe como argumento el valor que queremos tener entre los elementos

console.log(frasecompleta)
